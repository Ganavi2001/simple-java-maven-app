<settings>
  <!-- Other settings -->

  <servers>
    <server>
      <id>nexus-repo</id>
      <username> ${{ secrets.MAVEN_USERNAME }}</username>
      <password> ${{ secrets.MAVEN_PASSWORD }}</password>
    </server>
  </servers>

  <!-- Other settings -->
</settings>
