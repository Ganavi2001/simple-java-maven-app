<settings>
  <!-- Other settings -->

  <profiles>
    <profile>
      <id>nexus-profile</id>
      <repositories>
        <repository>
          <id>nexus-repo</id>
          <url>http://localhost:8081/repository/release/</url>
        </repository>
      </repositories>
    </profile>
  </profiles>
  
  <activeProfiles>
    <activeProfile>nexus-profile</activeProfile>
  </activeProfiles>

  <!-- Other settings -->
</settings>
